# Fixture — topic_query.sh composes a project-local tag registry

`signer:: agent/composer` · `at:: abc1234`

`[TQ1] grade::proved` A claim tagged with a tag only this fixture's own
registry admits. `check:: true` `tags:: topic-query-tag`
