entries:
  - id: e-dangler
    statement: "An entry depending on an id no entry declares."
    assertion: claim
    backing: unclosed
    signer:
      kind: agent
      name: architect-seat
    depends:
      - e-ghost
    axes:
      determined: true
      certifiable: false
      monotone: true
