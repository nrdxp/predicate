# Question-only fixture — no claims, no register (must NOT be a violation)

`signer:: agent/worker` · `at:: bbbbbbb`

`[Q1] grade::dispatchable` **A question exists, but no claim.** AI5's ruling
gates the empty-register error on a corpus holding graded CLAIMS; a corpus
that has only asked something, and answered nothing yet, owes no register.
`discharge:: fixture discharge condition` `closer:: agent`
