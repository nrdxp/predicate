# Directions fixture — a direction with zero terminal questions (must be RED)

`signer:: agent/composer` · `at:: aaaaaaa`

Synthetic, standalone corpus for ledger/gate/test_directions_register.sh —
not the live `.ledger/directions.md`.

`[D1] grade::directive` **D1 is the fixture direction.** Non-terminal by
construction. It carries no `[D1-Txx]` terminal target anywhere in this
corpus.
