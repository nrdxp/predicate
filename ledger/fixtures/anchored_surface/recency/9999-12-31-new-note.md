# Synthetic recency fixture — the NEW document

`signer:: agent/test` · `at:: 2222222`

Paired with `1000-01-01-old-note.md`. This file sorts LAST alphabetically,
which is LAST chronologically under the same date-stem convention. No-anchor
recency ordering must prefer this document's entry over the older one when a
budget forces a choice between them.

`[NEW1] grade::frontier` The single open member of the newest document in the
recency fixture pair, sized to match its OLD-document counterpart exactly so
budget alone decides between them.
`discharge:: recency ordering, newer side` `closer:: agent/test`
