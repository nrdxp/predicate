# Synthetic recency fixture — the OLD document

`signer:: agent/test` · `at:: 1111111`

Paired with `9999-12-31-new-note.md`. The pair's only purpose is the file-path
date-stem: this file sorts FIRST alphabetically, which is FIRST
chronologically under the corpus's own `YYYY-MM-DD-topic.md` naming
convention (`.ledger/log/`). No-anchor recency ordering must prefer the
newer file's entry over this one when a budget forces a choice between them.

`[OLD1] grade::frontier` The single open member of the oldest document in the
recency fixture pair, sized to match its NEW-document counterpart exactly so
budget alone decides between them.
`discharge:: recency ordering, older side` `closer:: agent/test`
