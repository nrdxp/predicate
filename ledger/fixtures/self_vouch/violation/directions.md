# Directions fixture — violation

`signer:: agent/composer` · `at:: aaaaaaa`

Synthetic, standalone corpus for ledger/gate/test_self_vouch.sh. Not the live
`.ledger/directions.md`.

`[D1] grade::directive` **D1 is the fixture direction.** Non-terminal by
construction.

`[D1-T1] grade::directive` **D1-T1 is the fixture terminal target.**
`discharge:: fixture discharge condition` `closer:: agent`
