# Synthetic residual/awaiting-human fixture — the two views, side by side

`signer:: agent/composer` · `at:: abc1234`

A minimal pair for the `awaiting_human` view (node/ran-and-residual). One
routed question with a human closer names real work the head must clear; one
residual question with the SAME human closer is open by theorem — no work
exists for anyone to clear. Only `backing` differs between the two nodes, so
a view that fails to read it puts both in the queue, or drops both.

## Questions

`[R1] grade::routed` A genuine human decision: which threshold the team
adopts. `discharge:: the head states the threshold` `closer:: human/nrd`

`[Z1] grade::residual` Whether the pre-March flaky failures would have
survived this refactor — the runs were never recorded and the database they
ran against is gone. `discharge:: provably none; the evidence no longer
exists` `closer:: human/nrd`
