# Fixture — a project-local registry admits a tag the plugin default does not

`signer:: agent/composer` · `at:: abc1234`

`[T1] grade::proved` A claim tagged with a tag only this project's own
registry admits. `check:: true` `tags:: project-topic`
