# Fixture — a tag in neither registry is refused

`signer:: agent/composer` · `at:: abc1234`

`[T2] grade::proved` A claim tagged with a tag neither the plugin default
nor this project's own registry admits. `check:: true` `tags:: nowhere-tag`
