# Red fixture — a proved claim naming a check nobody ran

`signer:: agent/composer` · `at:: abc1234`

The exists/ran cut (docs/entries.md "The two evidence species"): a check
companion names a mechanism, but a proved claim closes only on one that RAN.
The node below names one and states no observed result, so it must be
reported and dropped rather than emitted corroborated on no evidence — the
extractor cannot confirm a command executed, but it CAN refuse to infer a run
from the command's mere presence.

`[B1] grade::proved` Sharing one container brings the suite to 2m40s.
`check:: time pytest tests/` `tags:: perf`

`[B2] grade::proved` A well-formed sibling, naming its observed result, still
extracts beside the report: one unrun check costs its own node and nothing
standing next to it.
`check:: time pytest tests/` → 2m40s, 412 passed, 0 failed.
