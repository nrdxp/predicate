# Red fixture — `source:: same` with nothing before it must be reported

`signer:: agent/composer` · `at:: abc1234`

`[K1] grade::cited` The anaphora points at a source that does not exist;
resolving it silently would fabricate a witness. `source:: same`
