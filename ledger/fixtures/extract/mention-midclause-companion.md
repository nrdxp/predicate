# Mention fixture — a token quoted mid-sentence is still prose, not a field

`signer:: agent/composer` · `at:: abc1234`

`[K1] grade::synthesis` It should not be folded. The bullet explains what
`source:: same` means before the field is ever used here. The sentence keeps
going past it, describing the split rather than declaring a witness.
`derives-from:: [K2]`

`[K2] grade::proved` `check:: true`, verified against the same worked example
`tags:: D1`
