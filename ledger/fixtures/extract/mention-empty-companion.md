# Mention fixture — known tokens named in prose, never used

`signer:: agent/composer` · `at:: abc1234`

`[K1] grade::proved` The gate passes. `check:: true` In prose about the
grammar itself: the `discharge::` token is the prospective half, the
`closer::` token routes it, a bare `source::` span names the witness field,
and a late bare `check::` span must not clobber the run recorded above.
