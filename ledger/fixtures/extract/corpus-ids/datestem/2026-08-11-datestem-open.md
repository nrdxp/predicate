# Date-prefixed stems — the earlier document

`signer:: agent/composer` · `at:: 5b2e7d4`

This fixture is SYNTHETIC and is extracted together with
2026-08-12-datestem-closer.md. Its file name is the shape every document in
the landed record actually carries: a date, then a slug. The other document
reaches both nodes below by that name, so the stem a qualified reference may
carry is exercised here as the record writes it rather than as the other
fixtures spell it.

## Claims

`[K1] grade::cited` The target of the cross-document derivation edge.
`source:: the ratification log` `axes:: +determined -certifiable -monotone`
`freshness:: re-read the ratification log at reconcile`

## Questions

`[R1] grade::routed` The question the later document answers.
`discharge:: the head rules` `closer:: human/nrd`
