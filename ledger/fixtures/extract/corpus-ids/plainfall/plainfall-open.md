# The document a plain reference must not reach

`signer:: agent/composer` · `at:: 3f1c9a2`

This fixture is SYNTHETIC and is extracted with plainfall-closer.md. Its one
question carries the marker the plain edge next door names, and it must stay
unreachable by that edge.

## Questions

`[Z9] grade::routed` The marker a plain reference must not reach.
`discharge:: the head rules` `closer:: human/nrd`
