# Wikilinks stay external — the untouched document

`signer:: agent/composer` · `at:: 3f1c9a2`

This fixture is SYNTHETIC and is extracted with wikicap-closer.md. Its one
question is the silent-capture target: the other document names it inside a
wikilink, and the record must still show it open.

## Questions

`[R1] grade::routed` The question a wikilink cannot close.
`discharge:: the head rules` `closer:: human/nrd`
