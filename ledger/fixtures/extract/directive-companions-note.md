# Synthetic fixture — every companion a directive can legitimately carry, and one it cannot

`signer:: agent/composer` · `at:: abc1234`

This fixture is SYNTHETIC and pins the extractor's directive branch against
the silent-drop defect (a discharges span on a directive used to vanish
before this fix, per ruling AI13's discipline applied one level down —
never omit, let the type layer judge). Every companion attach_companions
recognizes is exercised on the directive node below, the closure edge
included: the extractor never judges admissibility; that is entry.ncl's
Directive/DirectiveClosesByAuthority job, exercised at the contract layer
in the entry fixture suite instead.

`[K1] grade::proved` A claim the directive below cites as its ground.
`check:: true` → EXIT=0

`[Q1] grade::dispatchable` A question the directive below wrongly claims to
discharge — a directive has no evidence to close anything with.
`discharge:: nothing needs to happen for this fixture` `closer:: agent`

`[D1] grade::directive` Every companion the grammar recognizes lands on the
node, not only its provenance.
`provenance:: architect ruling, this session` `discharge:: draft the fix and
land it` `closer:: agent` `derives-from:: [K1]` `tags:: D1`
`discharges:: [Q1]`
