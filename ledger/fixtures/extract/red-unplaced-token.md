# Red fixture — a vocabulary token outside a node marker must be reported

`signer:: agent/composer` · `at:: abc1234`

The suite went red on grade::proved yesterday, says this prose — a full
vocabulary token with no `[ID]` marker, which the census counts but no node
collects.

`[K1] grade::cited` A well-formed sibling still extracts, though its unknown
companion token is reported rather than silently discarded.
`source:: the head` `exit:: unknown — see K9`
