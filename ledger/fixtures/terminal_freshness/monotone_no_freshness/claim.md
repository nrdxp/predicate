# Claim fixture — monotone, satisfies D1-T1, no freshness owed (must NOT be a violation)

`signer:: agent/worker` · `at:: bbbbbbb`

`[C1] grade::proved` **C1 satisfies D1-T1 and is monotone, so it owes no
freshness cure.** `check:: fixture check` `source:: agent/other-party`
`axes:: +determined +certifiable +monotone` `discharges::
[directions:D1-T1]`
