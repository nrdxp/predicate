# A claim tagged with a word nobody admitted

`signer:: agent/core-worker` · `at:: 4f2a91c`

`[B2] grade::proved` **The CI cache key omits the lockfile hash.**
`check:: grep -n 'key:' .github/workflows/ci.yml` → line 34. `tags:: speedup`
